#include <stdio.h>

/**
 * main - prints all base 10 numbers,
 * Return: 0 if executed correctly
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
