#include <stdio.h>

/**
 * main - prints numbers 1 to 10
 * Return: 0 if successfully executed
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
