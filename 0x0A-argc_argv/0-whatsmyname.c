#include <stdio.h>

/**
 * main - prints the name
 * @argc: offset count
 * @argv: offset value
 * Return: 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
